USE SpotifyClone;
CREATE FUNCTION  quantidade_musicas_no_historico(nome)
RETURNS INT DETERMINISTIC
BEGIN
SELECT
historico FROM SpotifyClone.tabela AS quantidade_musicas_no_historico
WHERE
END $$
DELIMITER;
